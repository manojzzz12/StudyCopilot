services:
  - type: web
    name: studycopilot-embeddings
    env: python
    plan: free
    rootDir: embedding-service
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn main:app --host 0.0.0.0 --port $PORT